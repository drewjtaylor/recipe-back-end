

const translateToQuery = (input) => {
    if (typeof input === String) {
        return input
    } else if (
        typeof input === Array) {
            let output = '';


            // 
        }
}


console.log(translateToQuery('Just a string'))

console.log(translateToQuery(['apple', 'banana']))